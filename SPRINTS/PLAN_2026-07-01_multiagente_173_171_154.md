# RUN_2026-07-01_0254 — plan `/multiagente --unattended Instelec#173 Instelec#171 Instelec#154`

Los 3 issues son elegibles mecánicamente (`autonomy.py`: `ruta ∈ {fix_path, sprint_path}`).
Sin bloque `CONTEXTO HITL` en la invocación → el split de scope de la épica (#171) se
decide acá, en el orquestador, con el criterio: ejecutar lo que es código-only, diferir
explícitamente lo que depende de insumo externo real (no adivinar).

## Instelec#173 — fix_path, trivial, riesgo bajo — CONTINUACIÓN

Bug raíz (header 'AvísoSAP'→'Aviso SAP') ya resuelto y **deployado en prod** (rev
`instelec-api-00171-zpl`, 100% tráfico), PR #180 **squash-merged** a `main`
(commit `336860b`). La branch local `fix/instelec-173` quedó huérfana (sus 2 commits
NO son ancestros de `origin/main` tras el squash) → se **eliminó** y F3 arranca
**fresh branch desde `origin/main`**.

**Entregables de esta iteración** (tabla anti-FIX_INCOMPLETO):

| # | Entregable | Evidencia esperada | ✅/❌ |
|---|---|---|---|
| 1 | Header 'Cuadrilla' → 'Tipo Consignación' en plantilla descargable (col F: header, ejemplo, ancho, instrucciones) | Excel descargado en prod muestra 'Tipo Consignación' con valores CC/CP | ⏳ |
| 2 | Columna 'Prioridad' eliminada de la plantilla (header, ejemplo, ancho, instrucciones, renumerar Descripción H→G) | Excel descargado en prod NO tiene columna Prioridad | ⏳ |
| 3 | NO tocar `Actividad.prioridad` (campo real persistido, usado en filtros/UI) | Grep confirma campo intacto | ⏳ |
| 4 | Test `tests/unit/test_issue_173.py` actualizado (headers hardcodeados obsoletos) | Test pasa con nuevos headers | ⏳ |

Sin gate de BD (solo cambia generación de archivo Excel, sin escritura a BD).

## Instelec#154 — fix_path, simple, riesgo bajo

Módulo "Resumen de Materiales" ya en prod (2 rondas de validación, 1 bounce ya resuelto).
QA de @Indunnova (29-jun) es mayormente ✅ con 2 observaciones puntuales:

| # | Entregable | Evidencia esperada | ✅/❌ |
|---|---|---|---|
| O1 | Investigar T-5 (~21.000 kg acero vs ~800-950 kg promedio) — dato real vs error de carga | Query BD confirma/descarta anomalía + reporte en comentario | ⏳ |
| O2 | Invertir orientación tabla: Materiales=filas / Torres=columnas + columna Total (hoy es al revés, 29 cols, scroll horizontal) | Vista `/construccion/.../resumen-materiales/` en prod muestra tabla invertida con scroll vertical | ⏳ |

Sin gate de BD (O1 es solo lectura; O2 es cambio de template+view).

## Instelec#171 — sprint_path (epic) — SCOPE SPLIT (4ª triage consecutiva, mismo veredicto)

**Ejecuta AHORA (Sprint A — código-only, backend ya completo):**

| # | Entregable | Evidencia esperada | ✅/❌ |
|---|---|---|---|
| A1 | Implementar `torre_form.html` real (activar create/edit de torres — `TorreCreateView`/`TorreEditView` ya tienen `fields`+`form_valid` completos) | Crear una torre nueva vía UI en proyecto QA (Puerta de Oro) funciona end-to-end | ⏳ |
| A2 | Confirmar que `numero` acepta alfanumérico (T-1A, T-1B) en el form (backend ya es CharField) | Crear torre con número alfanumérico se guarda y ordena correctamente | ⏳ |
| A3 | Verificar que el conteo 64/65 se resuelve solo (consecuencia de A1, sin cambio de código en SocialPredial/AmbientalView) | Conteo en prod refleja el número real de torres tras poder crear la torre 65 | ⏳ |

**Diferido explícitamente (NO adivinar — insumo externo real faltante):**
- **V3** (¿'No aplica' alcanza o necesita 'Anulada' separado?) — Instelec#149 ya rebotó
  **5 veces** tocando este mismo toggle sin claridad de intent. Requiere respuesta de Gabriel.
- **Sprint B — columnas configurables (V4-V7)** — feature nueva de cero, bloqueada hasta
  tener 2 ejemplos del "instructivo Hochiminh" de proyectos distintos (dato que el cliente
  debe proveer, no existe en el repo ni se puede inferir).
- `entrega.html` (EntregaElectromecanica) — reportado por QA pero **fuera de scope
  semántico** de #171 (issue nunca lo menciona).

El comentario de cierre (F6) debe declarar 🟡 (no 🟢, quedan sub-items pendientes) y
dejar las 3 preguntas explícitas a Gabriel/Miguel documentadas en `Instelec_171_f1.json`.

## Gate de BD (deploy_gate.py) por issue

Ninguno de los 3 requiere `requiere_BD_prod_escritura=true` (todo código + eventual
migración de metadata, no data-fix directo). Deploy autónomo esperado para los 3 tras F3.
